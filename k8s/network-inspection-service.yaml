apiVersion: v1
kind: Service
metadata:
  name: network-inspection
  labels:
    app: network-inspection
spec:
  type: ClusterIP
  selector:
    app: network-inspection
  ports:
    - name: http
      port: 80
      targetPort: 80
